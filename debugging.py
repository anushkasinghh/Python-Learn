def add_numbers(a,b,c,d):
	import pdb; pdb.set_trace()
	return a + b + c + d

add_numbers(1,2,3,4)
# import pdb
# pdb.set_trace()

# on one line :
#  import pdb; pdb.set_trace()

# common pdb commands:
# n(next line)
# l(list)
# c(continue, exits the code)
# p(print)